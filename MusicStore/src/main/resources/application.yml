spring:
  sql:
    init:
      schema-locations: classpath:/schema.sql

---
spring:
  config:
    activate:
      on-profile: dev
  sql:
    init:
      data-locations: classpath:/data.sql
server:
  port: 8080
---
spring:
  config:
    activate:
      on-profile: test
  sql:
    init:
      data-locations: classpath:/dataTest.sql
server:
  port: 8088